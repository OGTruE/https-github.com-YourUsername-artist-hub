# Artist Hub — Web App (Vite + React + Tailwind)

Repo-ready starter for O.G True:
- React + Vite + Tailwind front-end
- Firebase (Firestore + Storage) integration
- Firebase Auth (Google) for admin
- Stripe Checkout serverless endpoints (Vercel)

## Quick start
1. Copy `.env.example` to `.env` and fill values.
2. `npm install`
3. `npm run dev`

Deploy: Use Vercel. Set environment variables in the Vercel dashboard (see .env.example).
